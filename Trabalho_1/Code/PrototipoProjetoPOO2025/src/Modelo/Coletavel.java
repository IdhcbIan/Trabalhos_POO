package Modelo;

/**
 * Interface to represent collectible items in the game.
 */
public interface Coletavel {
    /**
     * Checks if the item has been collected.
     * @return true if collected, false otherwise
     */
    public boolean foiColetado();
}